## Basic Port Scanner

**What it does:** 
A local network utility designed to scan a contiguous range of TCP ports on a target host. It identifies open services and filters out closed ports, saving the findings to an output log for further security assessment.

**Requirements:**
- Python 3.x
- Standard `socket` module

**How to run:**
```bash
python port_scanner.py